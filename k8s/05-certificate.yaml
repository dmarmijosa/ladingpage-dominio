apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dmarmijosa-tls
  namespace: danny-armijos
spec:
  secretName: dmarmijosa-tls
  duration: 2160h
  renewBefore: 360h
  dnsNames:
    - dmarmijosa.com
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
