apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod # Nombre del emisor
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: dmarmijosa@gmail.com # ¡Tu email!
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
    - selector:
        dnsNames:
        - 'dmarmijosa.com' # ¡Tu dominio!
      http01:
        ingress:
          class: nginx
          ingressTemplate:
            metadata:
              annotations:
                cert-manager.io/issue-temporary-certificate: "true"